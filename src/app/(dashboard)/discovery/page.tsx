import { PhasePlaceholder } from "@/components/shared/phase-placeholder";

export default function DiscoveryPage() {
  return (
    <PhasePlaceholder
      title="Discovery"
      phase="Prompt 2"
      description="Discovery engines, geography planner and search-seed yield tracking are implemented in Phase 2. This screen will show provider health, geographic coverage, and high/low-yield seeds."
    />
  );
}

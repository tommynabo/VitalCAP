import { PhasePlaceholder } from "@/components/shared/phase-placeholder";

export default function SetterPage() {
  return (
    <PhasePlaceholder
      title="AI Setter"
      phase="Prompt 4"
      description="Reply ingestion, deterministic pre-router, LLM classification and the setter dashboard (pending review, branch accuracy, response latency) are implemented in Phase 4. Human review is required for every send at launch."
    />
  );
}

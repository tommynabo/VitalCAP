import { PhasePlaceholder } from "@/components/shared/phase-placeholder";

export default function AnalyticsPage() {
  return (
    <PhasePlaceholder
      title="Analytics"
      phase="Prompt 5"
      description="The full discovery-to-meeting funnel, broken down by engine, campaign, business type, province, channel and contact type, ships in Phase 5 on top of the data model from Phases 1–4."
    />
  );
}

import { PhasePlaceholder } from "@/components/shared/phase-placeholder";

export default function ReviewsPage() {
  return (
    <PhasePlaceholder
      title="Reviews"
      phase="Prompt 4 + Prompt 5"
      description="The three-column review inbox (conversation list, thread, AI analysis + draft controls) is implemented in Phase 4/5. No send action exists yet in Phase 0."
    />
  );
}

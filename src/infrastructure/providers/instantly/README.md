# infrastructure/providers/instantly

Owner: Phase 3. `EmailDeliveryProvider` adapter for Instantly. No campaign
ID is ever hard-coded — mapping lives in configuration/DB
(`campaign -> provider campaign ID`).

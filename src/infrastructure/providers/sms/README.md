# infrastructure/providers/sms

Owner: Phase 3. `SmsDeliveryProvider` adapter. No undocumented third-party
API (e.g. Textvy) is implemented until official documentation is available;
until then only the interface + mock adapter exist, clearly documented as
such in `docs/PROVIDERS.md`.

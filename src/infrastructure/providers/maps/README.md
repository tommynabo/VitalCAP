# infrastructure/providers/maps

Owner: Phase 2. `MapsDiscoveryProvider` adapter(s) behind the interface in
`domain/discovery/types.ts`. Real adapter implemented only against a
provider's official documentation; a mock/dev adapter always ships alongside
it so the product works without live credentials.

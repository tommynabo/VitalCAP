# services/routing

Owner: Phase 3 (Prompt 3 §3.2, §3.6). `ChannelRouter` — deterministic
endpoint/channel selection respecting priority order, eligibility, sender
capacity, concurrency (never owner + info@ simultaneously by default), and
desired channel mix. Empty in Phase 0.
